# Norrin AI Act Compliance Assistant
# Python 3.11+ recommended (tested on 3.13)

# UI
streamlit>=1.35.0

# Document ingestion (PDF, DOCX, PPTX, HTML, CSV, TXT, MD → Markdown)
markitdown[all]>=0.1.0

# Vector store + semantic retrieval
chromadb>=0.5.0

# Local embeddings (all-MiniLM-L6-v2 via Chroma); installs PyTorch transitively
sentence-transformers>=3.0.0

# LLM providers (Assessment + Critic agents; DeepSeek uses OpenAI-compatible API)
openai>=1.30.0
anthropic>=0.28.0

# Configuration
python-dotenv>=1.0.0
