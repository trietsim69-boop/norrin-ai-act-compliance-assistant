# Technical Overview

The chatbot uses a third-party large language model through an API.

The system retrieves approved help-centre articles and sends them to the LLM with the user's question. The LLM generates a response and a confidence score.

Escalation to a human customer support agent is planned but not yet fully specified.

