# Technical Overview

The spam filter uses message headers, sender reputation, link features, and message text to infer whether an email is likely spam.

Users can mark a message as not spam, restore it to the inbox, and add senders to an allow list.

The system does not use a general-purpose AI model and does not generate content for users.

